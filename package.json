{
  "dependencies": {
    "token-transformer": "^0.0.33"
  },
  "scripts": {
    "build": "token-transformer tokens.json output.json && npx style-dictionary build",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  }
}
